# ManimLib Creation Animation Module - 依赖配置文件
# ManimLib Creation Animation Module - Dependencies Configuration

# ═══════════════════════════════════════════════════════════════════════════════
# 📦 核心依赖 (Core Dependencies)
# ═══════════════════════════════════════════════════════════════════════════════

# 数值计算核心库 - 支持高性能数组操作和数学计算
numpy>=1.19.0,<3.0.0

# 官方ManimGL库 - 提供完整的manimlib功能
manimgl>=1.6.1  # Official ManimGL (provides manimlib)

# ═══════════════════════════════════════════════════════════════════════════════
# 🔧 可选依赖 (Optional Dependencies)
# ═══════════════════════════════════════════════════════════════════════════════

# 以下依赖为可选安装，根据具体使用场景决定是否需要：
# scipy>=1.7.0          # 科学计算库 - 高级数学函数和算法
# matplotlib>=3.3.0     # 绘图库 - 用于数据可视化和图表生成  
# pillow>=8.0.0         # 图像处理库 - 支持多种图像格式的读写和处理
# opencv-python>=4.5.0  # 计算机视觉库 - 图像和视频处理

# ═══════════════════════════════════════════════════════════════════════════════
# 🛠️ 开发依赖 (Development Dependencies)
# ═══════════════════════════════════════════════════════════════════════════════

# 以下依赖仅在开发环境中需要：
# pytest>=6.0.0         # 测试框架
# pytest-cov>=2.10.0    # 测试覆盖率
# flake8>=3.8.0         # 代码风格检查
# mypy>=0.800           # 静态类型检查
# black>=21.0.0         # 代码格式化
# isort>=5.0.0          # 导入语句排序

# ═══════════════════════════════════════════════════════════════════════════════
# 📋 安装说明 (Installation Instructions)
# ═══════════════════════════════════════════════════════════════════════════════

# 基础安装：
# pip install -r requirements_creation.txt

# 开发环境安装（包含所有可选依赖）：
# pip install -r requirements_creation.txt scipy matplotlib pillow opencv-python pytest pytest-cov flake8 mypy black isort

# 注意事项：
# 1. numpy版本兼容性：支持1.19.0到2.x.x版本
# 2. Python版本要求：建议使用Python 3.8+
# 3. 系统依赖：某些功能可能需要额外的系统级依赖
# 4. 虚拟环境：强烈建议在虚拟环境中安装以避免依赖冲突